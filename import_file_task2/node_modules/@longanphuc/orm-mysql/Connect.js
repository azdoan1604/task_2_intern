// connected database
const mysql = require('mysql2/promise');
module.exports = async (sql,param)=>{
    let conn = await mysql.createConnection(require('./config.json').Database)
    console.log(sql)
    console.log(param)
    let data = (await conn).execute(sql,param)
    conn.end()
    return data
}
